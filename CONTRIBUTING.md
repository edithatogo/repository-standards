# Contributing

Use a focused branch and pull request. Run the repository's validation command and attach evidence. Human approval is not required for this solo-maintainer estate; required automated checks must pass.
