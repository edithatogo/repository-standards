{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/edithatogo/repository-standards/schemas/verification-receipt.schema.json",
  "title": "Repository verification receipt",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "repository", "revision", "generated_at", "result", "checks"],
  "properties": {
    "schema_version": {"const": 1},
    "repository": {"type": "string", "pattern": "^[^/]+/[^/]+$"},
    "revision": {"type": "string", "minLength": 7},
    "generated_at": {"type": "string", "format": "date-time"},
    "seed": {"type": ["integer", "string", "null"]},
    "result": {"enum": ["pass", "fail", "partial"]},
    "checks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "result"],
        "properties": {
          "name": {"type": "string", "minLength": 1},
          "result": {"enum": ["pass", "fail", "skip"]},
          "duration_seconds": {"type": "number", "minimum": 0},
          "evidence": {"type": "string"},
          "reason": {"type": "string"}
        }
      }
    },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["path", "sha256"],
        "properties": {
          "path": {"type": "string"},
          "sha256": {"type": "string", "pattern": "^[a-fA-F0-9]{64}$"}
        }
      }
    },
    "quality_metrics": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "line_coverage_percent": {"type": "number", "minimum": 0, "maximum": 100},
        "branch_coverage_percent": {"type": "number", "minimum": 0, "maximum": 100},
        "patch_coverage_percent": {"type": "number", "minimum": 0, "maximum": 100},
        "mutation_score_percent": {"type": "number", "minimum": 0, "maximum": 100},
        "property_examples": {"type": "integer", "minimum": 0},
        "metamorphic_relations": {"type": "integer", "minimum": 0},
        "deterministic_simulation_scenarios": {"type": "integer", "minimum": 0},
        "contract_tests": {"type": "integer", "minimum": 0},
        "quarantined_tests": {"type": "integer", "minimum": 0},
        "oldest_quarantine_days": {"type": "integer", "minimum": 0},
        "benchmark_regressions": {"type": "integer", "minimum": 0}
      }
    }
  }
}
